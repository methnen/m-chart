# Duplicating Charts

M Chart does not include a built-in chart duplication feature. The recommended approach is to use a third-party duplication plugin such as [Yoast Duplicate Post](https://wordpress.org/plugins/duplicate-post/), which is compatible with the M Chart custom post type.

After duplicating, open the copy in the M Chart editor and modify the data or settings as needed.
